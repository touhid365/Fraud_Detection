# Backend MongoDB Configuration for Replit

```env
# Add these to Replit Secrets (click lock icon)

MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/fraud-detection
JWT_SECRET=generate-random-secret-key
NODE_ENV=production
PORT=5000
ML_MODEL_URL=http://localhost:5001
```

# Frontend Configuration for Replit

```env
REACT_APP_API_URL=https://your-backend-replit-url.replit.dev
```

# ML Model Configuration for Replit  

```env
FLASK_ENV=production
PORT=5001
```
